// NRPS - NON REPETATIVE PATTERN STRING
// 1. obtained by swapping the order that the given elements appear. 
#include<stdio.h>
#include<string.h>
int main(){
        int ch_n = 0;
        scanf("%d",&ch_n);
        int len = 0;
        scanf("%d",&len);
        char str[ch_n];
        fgets(str,sizeof(str),stdin);
        
        printf("The possible NRPS is : \n");
        
}